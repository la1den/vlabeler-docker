version: '3'
services:

  vlabeler:
    image: vlabeler:ds
    build: 
      context: .
      dockerfile: ./Dockerfile
    environment:
      - DISPLAY=${DISPLAY}
    network_mode: "host"
    volumes:
      - ./data:/data
      - /tmp/.X11-unix:/tmp/.X11-unix
